services:
  storyvord-frontend-dev:
    image: storyvord-frontend-dev:latest
    container_name: storyvord-frontend-dev
    build: .
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=development
    restart: unless-stopped
    networks:
      - storyvord-dev-network

networks:
  storyvord-dev-network:
    name: storyvord-dev-network
    external: true
